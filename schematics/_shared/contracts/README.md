# Shared Contracts

This folder contains canonical contracts used by all machine adapters.

- `gtm_event_v1`: normalized GTM event envelope.
- `cog_v1`: reusable cog definition contract.

Both JSON and YAML schema variants are provided for tooling portability.
